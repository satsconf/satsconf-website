export const metadata = {
    title: "Contato",
    description: "Entre em contato com a Satsconf",
  };
  

export default function ContatoLayout({ children }) {
    return (
      <>
      {children}
      </>
    )
  }